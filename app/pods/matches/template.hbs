{{title "Matches"}}

{{#cl-matches-container
  heroes=heroes
  matches=matches
  side=side
  sort=sort
  lanes=lanes
  role=role
  regions=regions
  duration=duration
  heroId=heroId
  includeLosses=includeLosses}}
  {{outlet}}
{{/cl-matches-container}}
